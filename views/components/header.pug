header
    div(class="wrapper flex flex-space-between flex-bottom")
        div
            div(id="sub-intro" class="h3") 某某大学
            if (homepage)
                div(id="intro" class="h2") 建言献策系统
            else
                a(id="intro" class="h2" href="/") 建言献策系统
        div
            if (user)
                a(class="general-link" href=`/profiles/${user.githubId}`) #{user.displayName}
                a(class="user-link logout" href="/logout") 登出
            else
                a(class="user-link" href="/login") 登录